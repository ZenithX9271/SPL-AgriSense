# requirements.txt

# --- Core Dependencies (Must be present for your imports) ---
streamlit
pandas
numpy
requests
geopy
sendgrid
argostranslate

# --- LLM/LangChain Dependencies (Fixing the import issue) ---
# Install the core LangChain library
langchain

# Install the Groq connector (which often pulls in 'langchain-community')
langchain-groq

# Install the community connector explicitly for safety
langchain-community

# Install the Groq SDK
groq

# Python-dotenv for local testing 
python-dotenv
